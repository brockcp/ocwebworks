NOTES:
Run server via NPM script (localhost:3000/src)
or,
load file and run NPM onchange

note: was getting error in devtools about sourcemapping of aos js file so deleted reference at bottom of src/js/aos.js after importing but before prod. build
